name: debounced_grouped_stream
description: >-
  A Dart stream wrapper that lets each subscriber independently debounce and
  group incoming events by key before delivery, with per-subscriber debounce
  duration and grouping configuration.
version: 1.0.0
repository: https://pub.dev/

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
